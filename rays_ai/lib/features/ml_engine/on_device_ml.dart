/// On-device ML Engine — PURGED
///
/// All local heuristic scoring has been removed.
/// The single source of truth for stress prediction is now the Python backend.
/// This file is kept as an empty shell so imports don't break during migration.
class OnDeviceMLEngine {
  // Intentionally empty — all scoring is done server-side.
}
